[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python3 blackroad-revenue-api.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[env]
PORT = "8000"
PYTHON_VERSION = "3.11"
